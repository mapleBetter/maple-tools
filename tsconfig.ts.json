{
  "compilerOptions": {
    "target": "ES2020", // 编译后的 JS 代码使用 ES2020 语法特性
    "outDir": "./dist", // 所有编译后的文件输出到 dist 目录
    "rootDir": "./ts", // 源代码的根目录为 ts 文件夹，保持目录结构
    "strict": true, // 启用所有严格类型检查，提高类型安全
    "module": "ESNext", // 使用 ESNext 模块系统，适合现代打包工具
    "moduleResolution": "node", // 按 Node.js 方式解析模块，支持第三方包
    "declaration": true, // 生成类型声明文件（.d.ts），用于类型提示
    "declarationDir": "./dist", // 类型声明文件输出到 dist 目录
    "esModuleInterop": true, // 允许 CommonJS 模块默认导入，兼容 import/export
    "skipLibCheck": true, // 跳过对依赖库的类型检查，提升编译速度
    "forceConsistentCasingInFileNames": true, // 文件名大小写必须一致，防止跨平台问题
    "resolveJsonModule": true, // 允许 import JSON 文件为模块
    "isolatedModules": false, // 是否每个文件单独编译，false 表示整体编译（适合库）
    "noEmit": false, // 是否禁止输出文件，false 表示允许输出
    "allowJs": false // 是否允许编译 JS 文件，false 表示只编译 TS 文件
  },
  "exclude": ["node_modules"], // 排除 node_modules 目录，不参与编译
  "include": ["ts/**/*.ts"] // 只编译 ts 目录下所有 .ts 文件
}
